apiVersion: batch/v1
kind: Job
metadata:
        name: webscraper
spec:
        template:
                metadata:
                        labels:
                                app: webscraper
                spec:
                        containers:
                                - name: webscraper
                                  image: 127.0.0.1:35000/webscraper:0.1
                                  ports:
                                          - containerPort: 5000
                        restartPolicy: OnFailure
